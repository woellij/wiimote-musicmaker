class WiiMotePositionMapper(object):
    def markers(self):

        markerLeftBottom = (0, 0)
        markerRightBottom = (1024, 0)
        markerLeftTop = (1024, 768)
        markerRigthTop = (0, 768)
        interactionRec = [(markerLeftBottom), (markerRightBottom), (markerRigthTop), (markerRigthTop)]



    # xlim(0, SRC_W)
    # ylim(0, SRC_H)
    # scatter(*zip(*scoords))  # repack points[] to axes[] for plotting
    # scatter([SRC_W / 2], [SRC_H / 2], c='r', marker='+')






    def __init__(self):
        pass

    def map(self, data):
        # TODO
        return (0, 0)
